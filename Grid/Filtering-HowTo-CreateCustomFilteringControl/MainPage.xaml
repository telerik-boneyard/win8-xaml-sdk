<Page
    x:Class="Filtering_HowTo_CreateCustomFilteringControl.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Filtering_HowTo_CreateCustomFilteringControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:telerikGrid="using:Telerik.UI.Xaml.Controls.Grid"
    mc:Ignorable="d">

    <Page.DataContext>
        <local:ViewModel/>
    </Page.DataContext>
    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <telerikGrid:RadDataGrid Width="500" AutoGenerateColumns="False" Height="500" ItemsSource="{Binding Data}">
            <telerikGrid:RadDataGrid.Columns>
                <telerikGrid:DataGridTextColumn PropertyName="Country"/>
                <telerikGrid:DataGridTextColumn PropertyName="Continent"/>
            </telerikGrid:RadDataGrid.Columns>
            <telerikGrid:RadDataGrid.Commands>
                <local:CustomFilterButtonTapCommand/>
            </telerikGrid:RadDataGrid.Commands>
        </telerikGrid:RadDataGrid>
    </Grid>
</Page>
